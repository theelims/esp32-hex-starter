[%- set p = module_part_number | parse_part_number -%]
[%- set rows = partition_layout(partition_scheme, p.flash_size_mb) -%]
# Auto-generated by Copier (scheme=[[ partition_scheme ]], flash=[[ p.flash_size_mb ]]MB).
# Re-scaffold rather than hand-tuning so flash size and scheme stay consistent.
# For partition_scheme=custom, edit the body below to fit your layout.
# Name, Type, SubType, Offset, Size, Flags
[% if rows is none -%]
# TODO(custom): fill in your partition layout here. The built-in schemes
# (default, ota, huge_app) are scaled to flash size automatically — pick
# one of those instead unless you truly need a bespoke layout.
[%- else -%]
[% for r in rows -%]
[[ r.name ]],[[ r.type ]],[[ r.subtype ]],[[ r.offset ]],[[ r.size ]],
[% endfor -%]
[%- endif %]
